security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: "auto"

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false

        main:
            lazy: true
            provider: app_user_provider

            json_login:
                check_path: api_login
                username_path: email
                password_path: password

            # Sans limite, on peut tester des mots de passe à l'infini. Trois compteurs dans
            # PostgreSQL (ADR 021, 022) : adresse + IP, IP seule, adresse seule.
            login_throttling:
                limiter: App\Security\LoginRateLimiter

            logout:
                path: api_logout
                invalidate_session: true

    access_control:
        # Tout ce qui est sous /api/owner exige un compte propriétaire connecté.
        - { path: ^/api/owner, roles: ROLE_OWNER }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
